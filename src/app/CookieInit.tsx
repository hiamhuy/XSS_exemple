"use client";

import { useEffect } from "react";

/** Goi API mot lan khi vao app de tao cookie demo (thay middleware). */
export function CookieInit() {
  // useEffect(() => {
  //   void fetch("/api/create-cookie", { method: "GET", credentials: "same-origin" });
  // }, []);

  return null;
}
